"""
Problem: Find All Pairs in List that Sum to Target
Write a function find_all_pairs(numbers, target) that takes a list of
numbers and a target sum, returning ALL pairs that add up to target.

Example:
  find_all_pairs([1, 2, 3, 4, 2], 6)   # Returns [(2, 4), (4, 2)]
  find_all_pairs([1, 1, 1], 2)          # Returns [(1, 1)]
  find_all_pairs([1], 2)                # Returns []

Constraints:
  - Each element can only be used once per pair
  - Return list of tuples
"""


def find_all_pairs(numbers, target):
    pass


# Tests
print(find_all_pairs([1, 2, 3, 4, 2], 6))   # [(2, 4), (4, 2)]
print(find_all_pairs([1, 1, 1], 2))          # [(1, 1)]
print(find_all_pairs([1], 2))                 # []
