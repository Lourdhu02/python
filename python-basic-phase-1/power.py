"""
Problem: Calculate Power Without Built-in
Write a function power(base, exponent) that calculates base raised to
exponent without using the ** operator or math.pow().

Example:
  power(2, 3)     # Returns 8
  power(5, 0)    # Returns 1
  power(2, -2)   # Returns 0.25

Constraints:
  - Handle positive exponents
  - Handle zero exponent
  - Handle negative exponents (return float)
"""


def power(base, exponent):
    pass


# Tests
print(power(2, 3))     # 8
print(power(5, 0))    # 1
print(power(2, -2))   # 0.25
print(power(10, 2))   # 100
