"""
Problem: Calculate Factorial Recursively
Write a function factorial(n) that calculates the factorial of a number
using recursion.

Example:
  factorial(5)    # Returns 120 (5! = 5*4*3*2*1)
  factorial(0)   # Returns 1
  factorial(1)   # Returns 1

Constraints:
  - Handle negative numbers (return None or raise error)
  - Use recursion, not iteration
"""


def factorial(n):
    pass


# Tests
print(factorial(5))   # 120
print(factorial(0))   # 1
print(factorial(1))   # 1
print(factorial(-1))  # None
