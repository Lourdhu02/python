"""
Problem: Check if Two Strings are Anagrams
Write a function are_anagrams(str1, str2) that returns True if two strings
are anagrams of each other, otherwise False.

Example:
  are_anagrams("listen", "silent")   # Returns True
  are_anagrams("hello", "world")     # Returns False
  are_anagrams("Dormitory", "Dirty room")  # Returns True

Constraints:
  - Case insensitive
  - Ignore spaces and punctuation
"""


def are_anagrams(str1, str2):
    pass


# Tests
print(are_anagrams("listen", "silent"))           # True
print(are_anagrams("hello", "world"))             # False
print(are_anagrams("Dormitory", "Dirty room"))    # True
print(are_anagrams("Eleven plus two", "Twelve plus one"))  # True
